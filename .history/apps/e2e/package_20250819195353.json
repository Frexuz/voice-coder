{
  "name": "e2e",
  "private": true,
  "scripts": {
  "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:watch": "playwright test --ui",
    "e2e": "PWTEST_WATCH=1 playwright test",
    "postinstall": "playwright install --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.2"
  }
}
